# Render blueprint — deploy from repo root with Docker (Angular + .NET).
# Presentation media is stored in PostgreSQL (no paid persistent disk required).
services:
  - type: web
    name: micale
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    branch: main
    healthCheckPath: /api/health
    autoDeploy: true
