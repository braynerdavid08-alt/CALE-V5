/* Local override when serving Angular without the API proxy (optional).
 * Copy to config.js only for special setups. Default public/config.js uses "".
 */
window.__CALE_CONFIG__ = { apiUrl: 'http://127.0.0.1:5000' };
