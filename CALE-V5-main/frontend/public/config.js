/* Runtime config for Mi CALE.
 * Same-origin (recommended): leave apiUrl as "".
 * Split API host: set apiUrl to "https://api.tudominio.com" (no trailing slash).
 */
window.__CALE_CONFIG__ = window.__CALE_CONFIG__ || { apiUrl: '' };
